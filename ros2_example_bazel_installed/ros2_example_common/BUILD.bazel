# -*- mode: python -*-
# vi: set ft=python :

load("@ros2//:rosidl.bzl", "rosidl_interfaces_group")

rosidl_interfaces_group(
    name = "ros2_example_common_msgs",
    interfaces = [
        "msg/Status.msg",
        "srv/Query.srv",
        "action/Do.action",
    ],
    tags = ["nolint"],  # ROS 2 does not follow Drake's styleguide
    visibility = ["//visibility:public"],
)
